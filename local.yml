---
- hosts: localhost
  connection: local
  become: true
  pre_tasks:
    - name: pre-run | update package cache ( RHEL )
      tags: always
      dnf: update_cache=yes
      changed_when: False
      when: ansible_distribution in ["Rockylinux", "Almalinux"]

- hosts: localhost
  become: true
  roles:
    - common

#- hosts: workstation
#  tags: workstation
#  become: true
#  roles:
#    - workstation
#
#- hosts: server
#  tags: server
#  become: true
#  roles:
#    - server
